#!/usr/bin/env python3

# Простой TCP-клиент
import socket

# Создаем сокет с использованием IPv4 и TCP
client = socket.socket(socket.AF_INET, socket.SOCK_STREAM)

# Подключаемся к серверу
client.connect(('localhost', 12345))

# Отправляем сообщение серверу
client.sendall("Привет от клиента!".encode())

# Получаем ответ от сервера
data = client.recv(1024)
print(f"Ответ от сервера: {data.decode()}")

# Закрываем соединение
client.close()

# Пояснение:
# - connect() устанавливает соединение с сервером.
# - sendall() отправляет данные серверу.
# - recv() получает данные от сервера.
# - close() закрывает соединение.

# Как протестировать:
# 1. Сначала запустите серверный скрипт.
# 2. Затем запустите клиентский скрипт в отдельном окне или терминале.
# 3. Вы увидите сообщения о передаче данных между клиентом и сервером.
